# riscv-toolchain-darwin-arm64

Download and unpack in /opt/riscv-toolchain/xuantie

```
cd M1s_BL808_SDK
mkdir -p toolchain/Darwin_arm64
ln -s /opt/riscv-toolchain/xuantie/bin toolchain/Darwin_arm64/bin
```
